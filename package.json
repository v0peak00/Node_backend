{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend example",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../../part1 && npm run build && cp -r dist ../backend/osa_3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aki",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.4",
    "morgan": "^1.10.1"
  }
}
